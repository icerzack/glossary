version: '3.8'

services:
  glossary:
    build:
      context: .
      dockerfile: Dockerfile
      cache_from:
        - glossary-app:latest
    container_name: glossary-app
    ports:
      - "63000:63000"
    volumes:
      - glossary-data:/data
    environment:
      - PORT=63001
      - DB_PATH=/data/glossary.db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:63001/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s

volumes:
  glossary-data:
    driver: local

